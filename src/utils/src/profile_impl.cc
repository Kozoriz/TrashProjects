#include "utils/profile_impl.h"

utils::ProfileImpl::ProfileImpl(const utils::String& file_name) {
  // TODO : read data from file
}

const utils::UInt utils::ProfileImpl::mover_adapters_count() const {
  return 3;
}
